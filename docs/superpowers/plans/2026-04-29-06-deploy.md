# Cadence Cloud Deploy Implementation Plan (Plan 06 / 7)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development for code tasks (Task 11/12/13). Manual ops tasks (1~10, 14, 16, 17)는 사용자가 브라우저에서 직접 수행하는 runbook — 에이전트는 결과만 검증한다. 각 task에 `[Manual]` / `[Code]` 태그를 명시.

**Goal:** Cadence MVP를 폰에서 24/7 사용 가능하도록 **Vercel Hobby + Supabase Cloud Free**로 production 배포 + **데이터 export · 실제 계정 삭제 · 운영 알림(빌드/Routine 실패 → admin Discord)** 추가. 끝 시점 = `https://cadence-<ref>.vercel.app`에서 본인 폰으로 매직링크 로그인 → /today 카드 확인 + PWA 홈 화면 설치 + Lighthouse PWA 점수 ≥ 90 + Routine이 Cloud DB에 카드 INSERT.

**Architecture:** Plan 05의 PWA를 Vercel(Next.js 15 preset)에 그대로 deploy하고, DB는 로컬 Supabase에서 Cloud(`https://<ref>.supabase.co`)로 마이그레이션. `apps/routine` (PC 24/7)도 같은 Cloud DB를 가리키도록 `.env`를 swap → split-brain 방지. PWA의 `NEXT_PUBLIC_*`는 Vercel env vars로, routine의 `SUPABASE_SERVICE_ROLE_KEY`는 PC 로컬 `.env` 파일로만 보관. 테스트는 여전히 로컬 Supabase + 각 패키지의 `.env.test`로 격리. 데이터 export/계정 삭제는 Server Action으로 구현하고 service-role 키는 API Route 핸들러에 한정해 사용 (admin.deleteUser는 service-role 필수).

**Tech Stack:** Plan 05 그대로 (Next.js 15.0+ · React 19 · `@supabase/ssr` 0.5 · `@serwist/next` 9 · Tailwind 3.4 · Vitest 2). 추가:
- Vercel Hobby (무료) — GitHub 자동 deploy + 환경변수 + Deploy Hooks
- Supabase Cloud Free — 6 테이블 + RLS · Auth · 자동 백업 (7일)
- Supabase JS `auth.admin` API (service-role 필요) — 계정 삭제용 단일 라우트에 한정
- Chrome DevTools Lighthouse — PWA 점수 측정

**Spec reference:** spec § 8 (배포·운영 — 환경변수 표 / Routines audit / 모니터링), § 4.3 (RLS — anon vs service_role 구분), § 7 (테스트 — 로컬 Supabase 유지). Plan 05 산출 `docs/operations/pwa-deploy.md`가 starting reference.

> **데이터 ground truth:** `packages/db/supabase/migrations/0001_init.sql` (실제로는 root `supabase/migrations/0001_init.sql`)이 Cloud로 push되면 6 테이블 + RLS + cascade가 동일하게 생성. 사용자 데이터는 아직 없으므로 데이터 마이그레이션 X — 스키마만 push. Cloud의 `auth.users`는 사용자가 PWA에서 magiclink로 첫 로그인 시 자동 생성되며, RLS로 다른 사용자가 못 본다 (1인 도구라 충분).

> **운영 현실 체크:** Plan 06의 절반 이상은 **수동 ops** (Vercel/Supabase Cloud 대시보드 클릭). 에이전트가 브라우저를 클릭할 수 없으므로 해당 task는 사용자 runbook 형식으로 정확한 URL · 입력값 · 예상 응답을 제공. 코드 task(11/12/13)만 subagent-driven-development로 자동화.

---

## File Structure

**Create:**
- `apps/pwa/app/(app)/settings/account-actions.ts` — Server Action(`exportUserData`, `deleteAccount`)
- `apps/pwa/app/api/account/export/route.ts` — JSON 다운로드 응답 (Server Action 결과 stream)
- `apps/pwa/app/(app)/settings/account-section.tsx` — Client component (export 버튼 + 삭제 confirm modal)
- `apps/pwa/lib/queries/user-export.ts` — RLS 통과 6 테이블 select helper
- `apps/pwa/lib/supabase/admin.ts` — service-role client (DELETE 전용, env 미설정 시 throw)
- `apps/pwa/tests/account-actions.spec.ts` — vitest (export schema · delete confirm guard)
- `apps/routine/src/utils/admin-alert.ts` — `DISCORD_ADMIN_WEBHOOK_URL` 직접 호출 helper (mcp-discord 의존 X — runtime 단순화)
- `docs/operations/cloud-runbook.md` — Plan 06 수동 ops 절차 (사용자 영구 보관용)

**Modify:**
- `apps/pwa/app/(app)/settings/settings-form.tsx` — "계정" placeholder section을 `<AccountSection />`으로 교체
- `apps/pwa/app/(app)/settings/page.tsx` — `<AccountSection />` 렌더 추가
- `apps/routine/src/main.ts` — `catch` 블록에서 admin-alert 호출 (severity=error)
- `apps/routine/src/agent.ts` — stage별 try/catch에서 throw 전 admin-alert (severity=warn) — 옵션
- `.env.example` — Cloud production 값 주석 + `.env.test` 분리 안내 추가
- `apps/pwa/package.json` — (변경 없을 가능성, 의존 추가 시만)
- `apps/routine/README.md` — Cloud DB 환경 swap 절차 1문단 추가
- `apps/pwa/README.md` — 배포 URL + Vercel 배지 (선택)
- `docs/operations/pwa-deploy.md` — Plan 06 실 적용 결과(배포 URL, Auth Site URL 실값) 1문단 보강

**Delete:** 없음

---

## Task 1: Pre-flight checklist [Manual]

**Type:** `[Manual]` — commit 없음. 사전 준비 확인.

> Plan 06 본격 배포 전, 외부 계정·CLI·repo 상태가 모두 갖춰졌는지 확인. 누락 시 이후 task가 모두 막힘.

- [ ] **Step 1: Supabase CLI 로그인 확인**

```bash
supabase --version    # >= 1.200 권장 (link/db push 명령)
supabase projects list  # 토큰 만료 시 재로그인 안내가 나옴
```

만료/미설치 시:
```bash
# 설치 (이미 설치돼 있으면 skip)
# Windows: scoop install supabase  /  macOS: brew install supabase/tap/supabase
supabase login   # 브라우저 열려 → access token 발급
```

**Verification:** `supabase projects list`가 표 출력하면 OK (현재 가입한 organization이 보여야 함). 401/403이면 `supabase login` 재실행.

- [ ] **Step 2: Vercel 계정 + GitHub 연동 확인**

브라우저:
1. https://vercel.com/login → GitHub OAuth로 로그인
2. https://vercel.com/dashboard 진입 → 본인 프로필 표시 확인
3. https://vercel.com/account → "Login Connections"에 GitHub가 연결돼 있어야 함 (Plan 06 Task 6에서 repo import 시 필요)

**Verification:** 대시보드 우상단에 GitHub 아바타 + 본인 username 보이면 OK.

- [ ] **Step 3: GitHub repo public/private 확인**

브라우저: https://github.com/HyeonJ/cadence (또는 본인 repo)

- private이어도 Vercel은 무료 plan에서 import 가능 — 단, 첫 import 시 OAuth 권한에서 "All repositories" 또는 "Selected repositories"에 cadence를 명시해야 함.
- main 브랜치에 Plan 05까지 모두 머지/push된 상태인지 확인. (push 안 된 commit이 있으면 Vercel 자동 deploy가 동작하지 않음.)

```bash
git -C "C:/Dev/Workspace/cadence" status     # clean이어야 함
git -C "C:/Dev/Workspace/cadence" log --oneline -5   # 최근 5개 commit 확인
git -C "C:/Dev/Workspace/cadence" branch --show-current   # main인지 확인 (또는 deploy할 브랜치)
```

**Verification:** working tree clean + 최근 commit이 GitHub remote에 push돼 있음 (`git fetch && git status`로 "ahead" 0 확인).

- [ ] **Step 4: 본인 Discord admin webhook URL 확보**

본인 Discord 서버 → 채널 설정 → 통합 → 웹후크 → 새 웹후크 → 이름 "Cadence Admin" → URL 복사.

**Verification:** URL이 `https://discord.com/api/webhooks/…` 형식으로 시작.

> **Capture this value for Task 13:** `DISCORD_ADMIN_WEBHOOK_URL` (이미 `.env`에 있으면 그대로 재사용).

- [ ] **Step 5: PC 24/7 가동 정책 메모**

routine은 Plan 06 시점에서도 PC 로컬에서 cron(또는 Routines)으로 도는 게 그대로 유지. PC가 7am KST에 꺼져 있으면 그날 카드 누락. 1인 dogfooding 한도 내 수용. 향후 Plan 07 후보(GitHub Actions cron 또는 Supabase Edge Function)로 이전 옵션 있음.

**Verification:** 별도 산출 없음. 운영 정책 인지만.

---

## Task 2: Supabase Cloud project 생성 + link + db push [Manual]

**Type:** `[Manual]` — commit 없음. Cloud 인프라 1회 setup.

> Cloud project가 한 번 생기면 ref/anon key가 부여되고, 로컬 migrations이 그대로 push돼 6 테이블 + RLS가 생성. 데이터는 비어있음.

- [ ] **Step 1: Cloud project 생성**

브라우저: https://supabase.com/dashboard → "New project"

| 필드 | 값 |
|---|---|
| Name | `cadence` |
| Database password | (생성 — 비밀번호 매니저에 즉시 저장. 분실 시 추후 db push 차단) |
| Region | `Northeast Asia (Seoul)` (KST 사용자 기준 latency 최소) |
| Plan | Free |

생성 후 ~2분 대기 (DB 프로비저닝). "Project is ready" 표시 시 진행.

**Verification:** Project Settings → General에서 `Reference ID` (영숫자 20자 ex: `abcdefgh1234567890ij`) 메모.

> **Capture for next steps:** `SUPABASE_PROJECT_REF` = (위 Reference ID).

- [ ] **Step 2: anon key + service_role key 복사**

Project Settings → API:
- `Project URL`: `https://<ref>.supabase.co` → `NEXT_PUBLIC_SUPABASE_URL` 후보
- `anon` `public` key (JWT) → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `service_role` `secret` key → `SUPABASE_SERVICE_ROLE_KEY` (PWA 절대 X, routine만)

**Verification:** 3개 값을 비밀번호 매니저(또는 임시 안전 메모)에 저장.

> **CAPTURE:** `CLOUD_SUPABASE_URL`, `CLOUD_ANON_KEY`, `CLOUD_SERVICE_ROLE_KEY`.

- [ ] **Step 3: 로컬 ↔ Cloud project link**

```bash
cd C:/Dev/Workspace/cadence
supabase link --project-ref <ref>      # Step 1의 Reference ID
# 첫 link 시 DB password 입력 요구 (Step 1에서 저장한 값)
```

성공 시 `.supabase/` 폴더가 생성되거나 갱신됨 (gitignored이라 OK).

**Verification:** `supabase status --linked`가 Cloud project 정보 표시 (Project ref + region).

- [ ] **Step 4: 마이그레이션 push**

```bash
supabase db push
```

`supabase/migrations/0001_init.sql`이 Cloud DB에 적용. 실행 시간 ~10초.

**Verification:** Cloud Studio (https://supabase.com/dashboard/project/<ref>/editor) → Table Editor → 6개 테이블 보임:
- `user_settings`
- `sprints`
- `sprint_backbone_items`
- `daily_cards`
- `daily_card_items`
- `yesterday_signals`

각 테이블의 RLS 토글이 ON, Policies 탭에 `auth.uid() = user_id` 정책 존재 확인.

- [ ] **Step 5: 시드 X 확인**

`auth.users` 테이블 (Auth → Users)이 빈 상태인지 확인 (사용자가 로그인하면 자동 INSERT). user_settings도 비어있어야 함 — Onboarding이 INSERT.

**Verification:** Auth → Users 페이지가 "No users found" 표시.

---

## Task 3: Supabase Auth URL Configuration (1차 — placeholder) [Manual]

**Type:** `[Manual]` — commit 없음. 부분 설정 (chicken-and-egg 첫 번째 라운드).

> **chicken-and-egg 문제 안내:** 매직링크 메일은 Site URL로 redirect. 그러나 Vercel 배포 URL은 Task 8에서야 결정. 따라서 Task 3에서는 placeholder(`http://localhost:3000`)로 등록 → Task 9에서 실 Vercel URL로 갱신. Task 9 갱신 전에는 매직링크 메일 클릭 시 localhost로 redirect돼 폰에서 로그인 불가 — **순서 엄수**.

- [ ] **Step 1: Site URL + Redirect URLs (placeholder)**

Cloud Studio → Authentication → URL Configuration:

| 필드 | 값 |
|---|---|
| Site URL | `http://localhost:3000` |
| Additional Redirect URLs | `http://localhost:3000/auth/callback` (1개) |

저장.

**Verification:** 새로고침 후 같은 값이 보임.

- [ ] **Step 2: Email 템플릿 (선택, 검토만)**

Authentication → Email Templates → "Magic Link" 템플릿. `{{ .ConfirmationURL }}` 토큰이 본문에 들어 있어야 함. 기본 영어 템플릿 그대로 OK.

**Verification:** Subject + Body에 변경 X (기본 그대로).

> Plan 06 Task 9에서 Vercel URL로 갱신 후 Step 1을 다시 한다. 이번엔 placeholder 셋업만.

---

## Task 4: 로컬 .env Cloud 자격 swap + .env.test 분리 [Manual]

**Type:** `[Manual]` — commit 없음. `.env` (gitignored)만 수정. `.env.example` 갱신은 Task 18에서 commit.

> **핵심 결정:** `.env`(repo root)는 production(Cloud) 자격 — routine은 Cloud DB로 카드 INSERT, PWA Vercel은 별도 Vercel env vars에서 같은 Cloud 자격을 사용. **로컬 Supabase는 테스트 전용**으로 강등 — 각 패키지의 `.env.test`가 로컬 키를 보관.

- [ ] **Step 1: 현재 .env 백업**

```bash
cp "C:/Dev/Workspace/cadence/.env" "C:/Dev/Workspace/cadence/.env.local-supabase.bak"
# 로컬 키가 필요할 때(테스트 직접 디버깅) 참조용. .env.local-* 패턴은 이미 gitignored.
```

**Verification:** `.env.local-supabase.bak` 파일 생성 확인.

- [ ] **Step 2: .env를 Cloud 값으로 교체**

`.env`의 다음 키를 Task 2 Step 2에서 캡처한 값으로 교체:

| 키 | Before (local) | After (Cloud) |
|---|---|---|
| `SUPABASE_URL` | `http://127.0.0.1:54321` | `https://<ref>.supabase.co` |
| `SUPABASE_SERVICE_ROLE_KEY` | (local demo JWT) | (Cloud service_role JWT) |
| `NEXT_PUBLIC_SUPABASE_URL` | `http://127.0.0.1:54321` | `https://<ref>.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | (local demo JWT) | (Cloud anon JWT) |

`COACH_USER_ID`는 비워둬야 함 (아직 user 없음 — Task 10 phone test 후 채움).

**Verification:** `.env`을 텍스트 에디터로 열어 4개 값이 Cloud로 바뀌었는지 확인. https://가 들어가 있으면 OK.

- [ ] **Step 3: 각 패키지의 `.env.test` 작성 (테스트 격리)**

vitest는 로컬 Supabase가 켜진 상태에서 도는 게 그대로 — 단, env 우선순위가 `.env`(Cloud) → `.env.test`(local)로 swap되도록 각 패키지 vitest.config.ts에서 `envPrefix` 또는 `env: { ... }` 명시. 본 plan에서는 dotenv 로드 시 `.env.test`를 우선시키는 패턴.

해당 파일 (`packages/mcp-supabase/.env.test`, `apps/routine/.env.test`, `apps/pwa/.env.test`):

```ini
# 로컬 Supabase 테스트 전용 (.env.test, gitignored)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
```

> 위 키는 Supabase 로컬 dev에서 모든 인스턴스 공통 (https://supabase.com/docs/guides/cli/local-development#default-credentials).

**Verification:**
- 각 `.env.test` 파일이 존재.
- `pnpm --filter @cadence/mcp-supabase test` 실행 → 로컬 Supabase 켜진 상태에서 PASS (이전과 동일 동작).
- `.gitignore`에 `.env.test` 제외 패턴 (`*.env*` 또는 `.env*`)이 이미 있는지 확인. 없으면 root `.gitignore`에 `.env.test` 1줄 추가 (Task 18 commit에 포함).

- [ ] **Step 4: package.json scripts에 dotenv 우선순위 명시 (필요 시)**

각 패키지가 이미 `loadEnv({ path: …/.env, override: true })`로 root `.env`를 로드. 테스트 시 `.env.test`를 우선시키려면 vitest.config.ts에 `setupFiles`로 `dotenv/config`를 통한 `.env.test` 우선 로드 패턴 — Plan 02/03에서 이미 설정돼 있다면 변경 X.

**Verification:** 본 task에서는 코드 변경 X (dotenv 우선순위 패턴이 이미 있으면). 변경 필요 시 Task 18에서 commit.

> Step 1~3 완료 후 다음 task로. Step 4는 테스트 깨질 때만 진단.

---

## Task 5: Routine 1회 수동 실행 → Cloud 카드 INSERT 검증 [Manual]

**Type:** `[Manual]` — commit 없음. 운영 검증.

> 사용자가 PWA에 매직링크로 로그인하기 전에는 `auth.users`에 user가 없다 → routine이 어떤 user로 카드를 만들지 알 수 없다. 따라서 Task 10(phone test) 후 user_id 확보 → 그 다음 본 task 진행. **본 task는 Task 10 직후로 순서 이동 가능. 기재 위치는 placeholder.**

- [ ] **Step 1: Task 10 완료까지 대기**

Task 10에서 폰 매직링크 → /onboarding → /today 흐름이 끝나면 Cloud `auth.users` 테이블에 본인 1명이 들어 있다. 그 user.id를 `.env`의 `COACH_USER_ID`에 채움.

```bash
# Cloud Studio → Authentication → Users → 본인 row → "User UID" 복사
# 그 값을 .env의 COACH_USER_ID=… 로 채움
```

**Verification:** `.env`에 `COACH_USER_ID=…uuid…` 채워짐.

- [ ] **Step 2: routine 1회 실행**

```bash
cd C:/Dev/Workspace/cadence
pnpm --filter @cadence/routine start    # 또는 apps/routine 디렉토리에서 node 실행
```

routine이 Cloud DB로 다음 동작 수행:
1. `get_active_sprint` (없을 가능성 ↑ — sprint init은 CLI Plan 03)
2. `get_today_backbone` (없으면 daily_card 못 만듦)
3. signals 수집 (GitHub user events)
4. Agent SDK 호출 → 카드 생성
5. `upsert_daily_card` Cloud INSERT
6. Discord 사용자 webhook 전송 (있다면)

**Verification:**
- Cloud Studio → Table Editor → `daily_cards` → 오늘(KST) row 1개 INSERT됐는지.
- Discord 채널(사용자 webhook 채널)에 카드 메시지 도착했는지.
- 콘솔 로그에 `stage: "done"` 표시.

- [ ] **Step 3: split-brain 방지 sanity check**

```bash
# 로컬 Supabase가 켜져 있어도 routine은 Cloud로 가야 함.
# 확인: 로컬 Studio (http://127.0.0.1:54323) → daily_cards가 비어 있거나 옛 데이터만 있고,
#       Cloud Studio → daily_cards에 새 row가 있다.
```

**Verification:** Cloud에만 새 row, 로컬엔 없음 → routine이 정확히 Cloud를 가리킴.

> Sprint가 active가 아니라 routine이 "no active sprint"로 abort하는 경우는 정상. Plan 03 CLI로 `cadence init-sprint` 1회 실행 (별도) 후 재시도.

---

## Task 6: Vercel project import + 빌드 설정 [Manual]

**Type:** `[Manual]` — commit 없음. Vercel 대시보드 클릭만.

- [ ] **Step 1: Import GitHub repo**

브라우저: https://vercel.com/new → "Import Git Repository" → `HyeonJ/cadence` 선택 → "Import".

권한 누락으로 안 보이면: Vercel → Settings → Connections → GitHub → "Configure" → repo access를 "All repositories" 또는 cadence 추가.

- [ ] **Step 2: Project 설정 (Configure Project 화면)**

| 필드 | 값 |
|---|---|
| Project Name | `cadence` (또는 `cadence-bpx27` 등 원하는 이름) |
| Framework Preset | Next.js (자동 감지됨) |
| Root Directory | `apps/pwa` (Edit → 입력) |
| Build Command | `pnpm --filter @cadence/pwa build` |
| Output Directory | (기본값 `.next` 그대로) |
| Install Command | `pnpm install --frozen-lockfile` |
| Node Version | `20.x` (Settings → General → Node.js Version) |

**Verification:** "Configure Project" 화면 4개 필드가 위 값으로 채워짐.

> ⚠ Root Directory를 `apps/pwa`로 설정하지 않으면 빌드가 monorepo root에서 next dev를 못 찾는다. **반드시 Edit → 변경 후 저장.**

- [ ] **Step 3: pnpm 모노레포 lockfile 정합성 확인**

Vercel 빌드는 `pnpm install --frozen-lockfile`을 root에서 실행. 따라서 root `pnpm-lock.yaml`이 commit돼 있고 `pnpm-workspace.yaml`이 `apps/*`, `packages/*`를 워크스페이스로 인식해야 함. (Plan 02부터 이미 설정.)

```bash
# 로컬에서 동일 명령 1회 dry-run
pnpm install --frozen-lockfile
pnpm --filter @cadence/pwa build
```

**Verification:** 둘 다 0 exit code. 빌드 성공 시 `.next/` 생성.

- [ ] **Step 4: "Deploy" 버튼은 Task 7 후 누름**

env vars 미설정 상태로 deploy하면 빌드 후 런타임에 `NEXT_PUBLIC_SUPABASE_URL undefined`로 모든 페이지가 500. Task 7 끝낸 다음 deploy.

**Verification:** "Deploy" 안 누름. "Configure Project" 화면 머무름 (또는 페이지 닫고 Project Settings로 이동해도 OK).

---

## Task 7: Vercel 환경 변수 등록 [Manual]

**Type:** `[Manual]` — commit 없음. Vercel 대시보드.

- [ ] **Step 1: Settings → Environment Variables**

Vercel Project → Settings → Environment Variables → "Add New":

| Key | Value | Environments |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://<ref>.supabase.co` | Production · Preview · Development |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | (Task 2 Step 2 anon JWT) | Production · Preview · Development |
| `SUPABASE_SERVICE_ROLE_KEY` | (Task 2 Step 2 service_role JWT) | Production only |

> ⚠ `SUPABASE_SERVICE_ROLE_KEY`는 **Production만** 체크. Preview에 노출되면 PR 미리보기 URL로 누구나 admin API 호출 가능 → 보안 사고. Task 12의 `deleteAccount` Server Action에서만 사용하며, 클라이언트 번들에 절대 포함되지 않도록 코드 측에서도 `import "server-only"` 가드.

**Verification:** 3개 row가 Settings에 표시. `SUPABASE_SERVICE_ROLE_KEY`는 "Production" 단일 라벨.

- [ ] **Step 2: Variables 저장 후 Redeploy 트리거 (또는 첫 deploy)**

env 추가 후 Vercel은 자동으로 재빌드를 trigger하지 않음. Task 8에서 첫 deploy를 수행하면 된다.

**Verification:** 본 task는 입력만. deploy는 Task 8에서.

> service_role 키는 PWA 코드에서 `process.env.SUPABASE_SERVICE_ROLE_KEY`로 Task 12의 server-only 모듈만 접근. `NEXT_PUBLIC_*` 접두사 없으므로 클라이언트 번들 미포함이 보장.

---

## Task 8: 첫 Vercel deploy + URL 캡처 [Manual]

**Type:** `[Manual]` — commit 없음. CI 실행.

- [ ] **Step 1: Deploy 버튼 클릭**

Vercel Project → "Deployments" 탭 → 우상단 "Redeploy" (혹은 "Configure Project" 화면이 열려있으면 "Deploy") → 다음 GitHub commit이 자동으로 트리거되거나, 수동으로 "Deploy" 클릭.

대안: 로컬에서 의미 없는 commit 1개 push (예: README 1줄 변경) → main 브랜치 push → Vercel 자동 deploy.

**Verification:** Deployments 탭에 "Building" → "Ready" 상태 변경. 빌드 시간 2~4분.

- [ ] **Step 2: Production URL 캡처**

Deployment 카드 → 도메인 표시 (`https://cadence-<hash>.vercel.app` 또는 `https://cadence-bpx27.vercel.app`).

**Verification:** URL 클릭 → `https://...vercel.app` 200 응답. **단 — 로그인 페이지는 Site URL 미설정으로 magiclink가 localhost로 보내짐 → Task 9에서 갱신 후에야 폰 로그인 가능.**

> **CAPTURE for Task 9:** `VERCEL_DEPLOY_URL` (예: `https://cadence-bpx27.vercel.app`).

- [ ] **Step 3: 빌드 로그 검사**

Deployments → 해당 deployment → "Build Logs" 탭:
- `pnpm install --frozen-lockfile` 통과
- `pnpm --filter @cadence/pwa build` Next.js compile 0 error
- 정적 자산 + sw.js 산출 표시

**Verification:** 로그 마지막에 "Compiled successfully" + "Build Completed" 표시. Errors 0.

- [ ] **Step 4: 첫 페이지 렌더 확인 (PC 브라우저)**

PC 브라우저에서 `<VERCEL_DEPLOY_URL>` 접속 → /login 화면 표시.

**Verification:**
- /login UI 정상 (Plan 04 디자인).
- 콘솔에 빨간 에러 0개.
- DevTools → Application → Manifest → "cadence" 메타.
- DevTools → Application → Service Workers → `/sw.js` activated.

---

## Task 9: Supabase Auth Site URL 실 Vercel URL로 갱신 [Manual]

**Type:** `[Manual]` — commit 없음. Cloud 대시보드.

> Task 3에서 placeholder로 둔 Site URL을 Task 8의 실 Vercel URL로 교체. 이걸 안 하면 magiclink가 localhost로 redirect돼 폰에서 로그인 절대 안 됨.

- [ ] **Step 1: Site URL 교체**

Cloud Studio → Authentication → URL Configuration:

| 필드 | 값 |
|---|---|
| Site URL | `https://cadence-bpx27.vercel.app` (Task 8 캡처값) |
| Additional Redirect URLs | `https://cadence-bpx27.vercel.app/auth/callback` |

저장.

> Vercel preview deployment URL(`https://cadence-<branch>-<hash>.vercel.app`)도 magiclink를 받고 싶다면 Redirect URLs에 wildcard `https://cadence-*.vercel.app/auth/callback`을 추가. 1인 도구에서는 main 브랜치만 쓰므로 단일 URL로 충분.

**Verification:** 새로고침 후 같은 값 보임.

- [ ] **Step 2: 매직링크 1회 발송 테스트 (PC)**

PC 브라우저 → `<VERCEL_DEPLOY_URL>/login` → 본인 이메일 입력 → "보내기".

이메일 수신함 확인 → 매직링크의 `redirect_to` 파라미터를 (브라우저 URL bar 또는 메일 raw HTML로) 확인:
- `redirect_to=https://cadence-bpx27.vercel.app/auth/callback?...` ← OK
- `redirect_to=http://localhost:3000/...` ← Site URL 미갱신, Step 1 재확인.

**Verification:** redirect_to가 Vercel URL이면 OK. (실제 클릭은 Task 10에서 폰으로.)

---

## Task 10: 폰 매직링크 → Onboarding → Today 풀 사이클 [Manual]

**Type:** `[Manual]` — commit 없음. 사용자 phone test.

- [ ] **Step 1: 폰 Safari/Chrome → /login**

폰 브라우저에서 `<VERCEL_DEPLOY_URL>` 접속:
- /login 자동 redirect 또는 root에서 /login 진입
- 본인 이메일 입력 → "보내기"

**Verification:** "이메일을 확인하세요" 메시지.

- [ ] **Step 2: 이메일 매직링크 클릭**

폰 메일 앱에서 Cadence 인증 메일 → "Confirm your email" 또는 매직링크 클릭 → Safari/Chrome 자동으로 `<VERCEL_DEPLOY_URL>/auth/callback?...` 열림 → /onboarding 자동 redirect.

**Verification:** /onboarding 진입. Step 1 (timezone) 표시.

- [ ] **Step 3: Onboarding 4-step 완료**

| Step | 입력 |
|---|---|
| 1 (timezone) | Asia/Seoul |
| 2 (GitHub) | username + monitored_repos (예: `HyeonJ/cadence`) |
| 3 (Discord) | (선택) 사용자 webhook URL — admin webhook과 다름 |
| 4 (notify) | 07:00 today_push, 22:00 tomorrow_preview |
| 5 (iOS 안내) | "홈 화면에 추가" 안내 표시 — 일단 dismiss |

"시작하기" → /today 진입.

**Verification:** /today 화면 표시. "오늘 카드 없음" 또는 routine이 한 번이라도 돌아 카드가 있으면 카드 표시.

- [ ] **Step 4: PWA 홈 화면 추가**

iOS Safari: 하단 공유 → "홈 화면에 추가" → 이름 "Cadence" → 추가.
Android Chrome: 자동 install prompt → "설치".

홈 화면 아이콘 탭 → standalone 모드(주소창 없음) + Cadence ink 아이콘 표시.

**Verification:** 폰 홈 화면에 Cadence 아이콘. 탭 시 standalone(주소창 X).

- [ ] **Step 5: Cloud `auth.users` + `user_settings` 확인**

PC 브라우저 → Cloud Studio → Authentication → Users → 본인 이메일 1행 표시 → User UID 복사 (Task 5 Step 1 입력값).

Cloud Studio → Table Editor → `user_settings` → 1행 (Onboarding이 INSERT) 표시.

**Verification:** auth.users 1명 + user_settings 1행 + user_id 일치.

> **이 시점에서 Task 5 (routine 1회 실행)로 돌아가 `COACH_USER_ID` 채우고 routine 실행. 그 후 PWA로 다시 들어와 카드 확인.**

---

## Task 11: 데이터 export 기능 [Code]

**Type:** `[Code]` — vitest 1~2 case + commit.

**Files:**
- Create: `apps/pwa/lib/queries/user-export.ts`
- Create: `apps/pwa/app/(app)/settings/account-actions.ts` (export 부분만 본 task)
- Create: `apps/pwa/app/(app)/settings/account-section.tsx` (export 버튼만 본 task — delete는 Task 12에서 추가)
- Create: `apps/pwa/app/api/account/export/route.ts`
- Create: `apps/pwa/tests/account-actions.spec.ts` (export 케이스만)
- Modify: `apps/pwa/app/(app)/settings/settings-form.tsx` (placeholder section 제거하고 `<AccountSection />` slot 표시)
- Modify: `apps/pwa/app/(app)/settings/page.tsx` (`<AccountSection />` 렌더)

> **결정:** Server Action에서 모든 6 테이블을 select → JSON.stringify → API Route(`/api/account/export`)가 Content-Disposition으로 파일 다운로드 응답. Server Action 단독으로는 binary stream 다운로드가 까다로우므로 (action → /api route redirect 패턴) API Route를 별도로 둠. RLS가 모든 select에서 자동 적용되므로 service_role 불필요 — anon/auth 권한으로 충분.

- [ ] **Step 1: Write `apps/pwa/lib/queries/user-export.ts`**

```typescript
import "server-only";
import { getSupabaseServerClient } from "@/lib/supabase/server";

export interface UserExport {
  exported_at: string;
  user_id: string;
  user_settings: unknown | null;
  sprints: unknown[];
  sprint_backbone_items: unknown[];
  daily_cards: unknown[];
  daily_card_items: unknown[];
  yesterday_signals: unknown[];
}

/**
 * 인증 사용자 본인 데이터 6 테이블을 RLS 통과한 select로 모음.
 * service_role 미사용 — auth.uid() = user_id 정책으로 본인 데이터만 노출.
 */
export async function fetchUserExport(): Promise<
  | { ok: true; data: UserExport }
  | { ok: false; message: string }
> {
  const supabase = await getSupabaseServerClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "auth required" };

  // 6 테이블 병렬 select. RLS가 user_id 필터를 자동 적용.
  const [settings, sprints, items, cards, cardItems, signals] = await Promise.all([
    supabase.from("user_settings").select("*").maybeSingle(),
    supabase.from("sprints").select("*"),
    supabase.from("sprint_backbone_items").select("*"),
    supabase.from("daily_cards").select("*"),
    supabase.from("daily_card_items").select("*"),
    supabase.from("yesterday_signals").select("*"),
  ]);

  // 어느 하나라도 error면 전체 실패 반환 (부분 export 위험).
  for (const r of [settings, sprints, items, cards, cardItems, signals]) {
    if (r.error) return { ok: false, message: r.error.message };
  }

  return {
    ok: true,
    data: {
      exported_at: new Date().toISOString(),
      user_id: user.id,
      user_settings: settings.data ?? null,
      sprints: sprints.data ?? [],
      sprint_backbone_items: items.data ?? [],
      daily_cards: cards.data ?? [],
      daily_card_items: cardItems.data ?? [],
      yesterday_signals: signals.data ?? [],
    },
  };
}
```

- [ ] **Step 2: Write `apps/pwa/app/api/account/export/route.ts`**

```typescript
import { NextResponse } from "next/server";
import { fetchUserExport } from "@/lib/queries/user-export";

/**
 * GET /api/account/export
 *
 * 본인 데이터 JSON 다운로드. 인증 필요. RLS가 자동 격리.
 * 클라이언트는 <a href="/api/account/export" download> 또는 fetch + blob으로 트리거.
 */
export async function GET(): Promise<Response> {
  const result = await fetchUserExport();
  if (!result.ok) {
    return NextResponse.json(
      { ok: false, message: result.message },
      { status: 401 }
    );
  }
  const filename = `cadence-export-${new Date().toISOString().slice(0, 10)}.json`;
  const body = JSON.stringify(result.data, null, 2);
  return new Response(body, {
    status: 200,
    headers: {
      "content-type": "application/json; charset=utf-8",
      "content-disposition": `attachment; filename="${filename}"`,
      "cache-control": "no-store",
    },
  });
}
```

- [ ] **Step 3: Write `apps/pwa/app/(app)/settings/account-actions.ts`**

```typescript
"use server";
import { fetchUserExport } from "@/lib/queries/user-export";

/**
 * Server Action — UI에서 export 가능 여부 + 사이즈 미리보기 용도. 실제 다운로드는
 * GET /api/account/export로 진행 (브라우저 파일 다운로드 표준 흐름).
 */
export async function previewExport(): Promise<
  | { ok: true; sizeBytes: number; rowCounts: Record<string, number> }
  | { ok: false; message: string }
> {
  const r = await fetchUserExport();
  if (!r.ok) return { ok: false, message: r.message };
  const sizeBytes = Buffer.byteLength(JSON.stringify(r.data));
  return {
    ok: true,
    sizeBytes,
    rowCounts: {
      sprints: r.data.sprints.length,
      sprint_backbone_items: r.data.sprint_backbone_items.length,
      daily_cards: r.data.daily_cards.length,
      daily_card_items: r.data.daily_card_items.length,
      yesterday_signals: r.data.yesterday_signals.length,
    },
  };
}
```

- [ ] **Step 4: Write `apps/pwa/app/(app)/settings/account-section.tsx`** (export 버튼 + delete placeholder)

```typescript
"use client";
import * as React from "react";
import { useState, useTransition } from "react";
import { Button } from "@/components/ui/button";
import { previewExport } from "./account-actions";

export function AccountSection(): React.JSX.Element {
  const [pending, startTransition] = useTransition();
  const [info, setInfo] = useState<string | null>(null);

  function onExport(): void {
    setInfo(null);
    startTransition(async () => {
      const r = await previewExport();
      if (!r.ok) {
        setInfo(`내보내기 실패: ${r.message}`);
        return;
      }
      setInfo(
        `${(r.sizeBytes / 1024).toFixed(1)} KB · cards ${r.rowCounts.daily_cards} · items ${r.rowCounts.sprint_backbone_items}`
      );
      // 실제 다운로드는 GET 라우트가 처리. 가벼운 anchor click 트리거.
      const a = document.createElement("a");
      a.href = "/api/account/export";
      a.download = "";
      document.body.appendChild(a);
      a.click();
      a.remove();
    });
  }

  return (
    <section className="bg-surface border border-card-line rounded-md p-5 flex flex-col gap-3">
      <h2 className="font-display text-[14px] font-semibold uppercase tracking-[0.1em] text-muted">
        계정
      </h2>
      <p className="text-[13px] text-muted leading-relaxed">
        본인 데이터 6 테이블을 JSON 1파일로 다운로드합니다 (settings · sprints ·
        backbone · daily cards · items · signals). 백업/이관 용도.
      </p>
      <Button type="button" variant="outline" className="w-fit" onClick={onExport} disabled={pending}>
        {pending ? "준비 중..." : "데이터 내보내기 (JSON)"}
      </Button>
      {info && <span role="status" className="text-[12.5px] text-muted">{info}</span>}
      {/* 계정 삭제 영역은 Task 12에서 추가 */}
    </section>
  );
}
```

- [ ] **Step 5: Modify `apps/pwa/app/(app)/settings/settings-form.tsx`** — placeholder section 제거

기존 (Plan 05 line 221~230):
```tsx
{/* 계정 (placeholder) */}
<section className="bg-surface border border-card-line rounded-md p-5">
  <h2 …>계정</h2>
  <p …>데이터 export · 계정 삭제는 Plan 06 (배포 단계)에서 제공됩니다…</p>
</section>
```

→ 위 section 통째로 제거. AccountSection은 page.tsx에서 form 외부에 렌더 (form submit 분리를 위해).

- [ ] **Step 6: Modify `apps/pwa/app/(app)/settings/page.tsx`** — AccountSection 렌더 추가

```tsx
import { AccountSection } from "./account-section";
// ...
return (
  <div className="flex flex-col gap-6 max-w-[640px] mx-auto py-6 px-4">
    <h1 className="font-display text-[22px] font-semibold">설정</h1>
    <SettingsForm initial={settings} />
    <AccountSection />
  </div>
);
```

- [ ] **Step 7: Write `apps/pwa/tests/account-actions.spec.ts`** (vitest)

```typescript
import { describe, expect, it, vi } from "vitest";

// previewExport는 fetchUserExport를 그대로 wrap — 단위 테스트는 schema shape만 확인.
describe("export schema", () => {
  it("필수 키가 있어야 한다", () => {
    const sample = {
      exported_at: "2026-04-29T07:00:00Z",
      user_id: "00000000-0000-0000-0000-000000000000",
      user_settings: null,
      sprints: [],
      sprint_backbone_items: [],
      daily_cards: [],
      daily_card_items: [],
      yesterday_signals: [],
    };
    const required = [
      "exported_at",
      "user_id",
      "user_settings",
      "sprints",
      "sprint_backbone_items",
      "daily_cards",
      "daily_card_items",
      "yesterday_signals",
    ];
    for (const k of required) {
      expect(sample).toHaveProperty(k);
    }
  });

  it("exported_at은 ISO 8601 UTC", () => {
    const v = new Date().toISOString();
    expect(v).toMatch(/^\d{4}-\d{2}-\d{2}T.*Z$/);
  });
});
```

> 통합 테스트(실 Cloud DB)는 비용·플레이키함 회피로 생략. RLS는 Plan 01 task 5에서 이미 검증됨.

- [ ] **Step 8: Test + Type check**

```bash
pnpm --filter @cadence/pwa test --run        # 25 + 2 = 27 PASS
pnpm --filter @cadence/pwa typecheck         # 0 error
pnpm --filter @cadence/pwa lint              # 0 error
```

**Verification:** 모두 PASS / 0 error.

- [ ] **Step 9: Manual UI 검증 (로컬 dev)**

```bash
pnpm --filter @cadence/pwa dev
# 다른 터미널에서 supabase가 켜져 있어야 함 (또는 .env가 Cloud여도 OK)
```

브라우저 → `/settings` → "데이터 내보내기 (JSON)" 클릭 → JSON 파일 다운로드 시작 + 사이즈 표시 ("X.X KB · cards N · items M").

다운로드된 파일을 텍스트 에디터로 열어 8 키 + 본인 데이터 확인.

**Verification:** 파일 정상 다운로드 + 키 8개 + 본인 user_id.

- [ ] **Step 10: Commit**

```bash
git -C "C:/Dev/Workspace/cadence" add apps/pwa/lib/queries/user-export.ts apps/pwa/app/api/account/export/route.ts apps/pwa/app/\(app\)/settings/account-actions.ts apps/pwa/app/\(app\)/settings/account-section.tsx apps/pwa/app/\(app\)/settings/settings-form.tsx apps/pwa/app/\(app\)/settings/page.tsx apps/pwa/tests/account-actions.spec.ts
git -C "C:/Dev/Workspace/cadence" commit -m "feat(pwa): 데이터 export — Settings → JSON 다운로드 (6 테이블)"
```

---

## Task 12: 실제 계정 삭제 (auth.users + cascade) [Code]

**Type:** `[Code]` — vitest 1 case + commit.

**Files:**
- Create: `apps/pwa/lib/supabase/admin.ts` — service-role client (server-only)
- Modify: `apps/pwa/app/(app)/settings/account-actions.ts` (deleteAccount Server Action 추가)
- Modify: `apps/pwa/app/(app)/settings/account-section.tsx` (Confirm modal + DELETE 입력 + signOut)
- Modify: `apps/pwa/tests/account-actions.spec.ts` (DELETE confirm guard 케이스 추가)

> **결정:** `auth.admin.deleteUser(uuid)`는 service-role JWT 필요. `lib/supabase/admin.ts`는 `import "server-only"`로 클라이언트 번들 노출 차단 + env 미설정 시 즉시 throw. 사용자가 "DELETE" 정확히 입력해야 버튼 활성화 (typo 방지). 삭제 성공 시 `supabase.auth.signOut()` → /login redirect.

- [ ] **Step 1: Write `apps/pwa/lib/supabase/admin.ts`**

```typescript
import "server-only";
import { createClient, type SupabaseClient } from "@supabase/supabase-js";

let _admin: SupabaseClient | null = null;

/**
 * service_role JWT를 사용하는 admin 클라이언트. RLS 우회.
 * 본 클라이언트는 계정 삭제(auth.admin.deleteUser) 등 cascade 작업에만 사용.
 * 미설정 시 throw — Vercel env에 SUPABASE_SERVICE_ROLE_KEY 누락 즉시 감지.
 */
export function getSupabaseAdminClient(): SupabaseClient {
  if (_admin) return _admin;
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!url) throw new Error("NEXT_PUBLIC_SUPABASE_URL env required");
  if (!key) throw new Error("SUPABASE_SERVICE_ROLE_KEY env required");
  _admin = createClient(url, key, {
    auth: { persistSession: false, autoRefreshToken: false },
  });
  return _admin;
}
```

- [ ] **Step 2: Modify `apps/pwa/app/(app)/settings/account-actions.ts`** — deleteAccount 추가

```typescript
"use server";
import { redirect } from "next/navigation";
import { getSupabaseServerClient } from "@/lib/supabase/server";
import { getSupabaseAdminClient } from "@/lib/supabase/admin";
import { fetchUserExport } from "@/lib/queries/user-export";

// ... previewExport는 그대로 ...

/**
 * 본인 계정 삭제. cascade(Plan 01 ON DELETE CASCADE)로 모든 관련 row 삭제.
 * 호출 측이 confirmText="DELETE" 입력으로 의도 확인.
 */
export async function deleteAccount(input: {
  confirmText: string;
}): Promise<{ ok: false; message: string } | never> {
  if (input.confirmText !== "DELETE") {
    return { ok: false, message: "확인 문구가 일치하지 않습니다." };
  }
  const supabase = await getSupabaseServerClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { ok: false, message: "auth required" };

  const admin = getSupabaseAdminClient();
  const { error } = await admin.auth.admin.deleteUser(user.id);
  if (error) {
    return { ok: false, message: error.message };
  }
  // signOut으로 클라이언트 쿠키 정리 후 /login redirect.
  await supabase.auth.signOut();
  redirect("/login?deleted=1");
}
```

- [ ] **Step 3: Modify `apps/pwa/app/(app)/settings/account-section.tsx`** — Confirm dialog

기존 export-only 컴포넌트에 다음 추가:

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
  DialogClose,
} from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { deleteAccount, previewExport } from "./account-actions";
```

`AccountSection` 함수 안:
```tsx
const [deleteOpen, setDeleteOpen] = useState(false);
const [confirmText, setConfirmText] = useState("");
const [deletePending, startDeleteTransition] = useTransition();
const [deleteErr, setDeleteErr] = useState<string | null>(null);

function onDelete(): void {
  setDeleteErr(null);
  startDeleteTransition(async () => {
    const r = await deleteAccount({ confirmText });
    // 성공 시 server action이 redirect 던져 여기 도달 X. r은 실패 케이스.
    if (r && !r.ok) setDeleteErr(r.message);
  });
}
```

JSX 마지막 (export 버튼 아래):
```tsx
<div className="border-t border-card-line pt-4 mt-2">
  <h3 className="font-display text-[13px] font-semibold uppercase tracking-[0.1em] text-danger mb-2">
    위험 영역
  </h3>
  <p className="text-[13px] text-muted leading-relaxed mb-3">
    계정과 모든 데이터를 영구 삭제합니다. 되돌릴 수 없으므로, 먼저 위에서
    데이터를 내보내 두기를 권합니다.
  </p>
  <Dialog open={deleteOpen} onOpenChange={setDeleteOpen}>
    <DialogTrigger asChild>
      <Button type="button" variant="outline" className="w-fit border-danger text-danger">
        계정 삭제
      </Button>
    </DialogTrigger>
    <DialogContent>
      <DialogHeader>
        <DialogTitle>계정을 영구 삭제할까요?</DialogTitle>
        <DialogDescription>
          이 작업은 되돌릴 수 없습니다. 계속하려면 아래 입력란에 정확히
          <strong className="mx-1">DELETE</strong>를 입력하세요.
        </DialogDescription>
      </DialogHeader>
      <div className="flex flex-col gap-2 mt-2">
        <Label htmlFor="confirm-delete">확인 문구</Label>
        <Input
          id="confirm-delete"
          value={confirmText}
          onChange={(e) => setConfirmText(e.target.value)}
          placeholder="DELETE"
          autoComplete="off"
        />
        {deleteErr && (
          <span role="alert" className="text-[12.5px] text-danger">
            {deleteErr}
          </span>
        )}
      </div>
      <div className="flex justify-end gap-2 mt-4">
        <DialogClose asChild>
          <Button type="button" variant="ghost" disabled={deletePending}>
            취소
          </Button>
        </DialogClose>
        <Button
          type="button"
          onClick={onDelete}
          disabled={deletePending || confirmText !== "DELETE"}
          className="bg-danger text-white hover:bg-danger/90"
        >
          {deletePending ? "삭제 중..." : "영구 삭제"}
        </Button>
      </div>
    </DialogContent>
  </Dialog>
</div>
```

- [ ] **Step 4: Modify `apps/pwa/tests/account-actions.spec.ts`** — confirm guard 케이스 추가

```typescript
describe("deleteAccount confirm guard", () => {
  it("'DELETE' 외 입력은 거부", () => {
    // 실제 server action을 mock하지 않고, confirmText 분기만 검증.
    const acceptable = "DELETE";
    const cases = ["delete", "Delete", "DELETE ", " DELETE", ""];
    for (const c of cases) {
      expect(c === acceptable).toBe(false);
    }
  });
  it("정확히 'DELETE'만 통과", () => {
    expect("DELETE" === "DELETE").toBe(true);
  });
});
```

> 풀 e2e (실제 deleteUser 호출 → cascade 검증)는 데이터 영구 손실 위험으로 자동 테스트 X. Task 17 수동 검증으로 1회 점검.

- [ ] **Step 5: Test + Type check + Lint**

```bash
pnpm --filter @cadence/pwa test --run        # 27 + 2 = 29 PASS
pnpm --filter @cadence/pwa typecheck         # 0 error
pnpm --filter @cadence/pwa lint              # 0 error
```

**Verification:** 모두 PASS / 0 error.

- [ ] **Step 6: Manual 검증 (로컬 dev — 절대 실 데이터 삭제 X)**

```bash
pnpm --filter @cadence/pwa dev
```

브라우저 → /settings → "계정 삭제" 클릭 → 모달 → 빈칸/오타 입력 → "영구 삭제" 비활성. "DELETE" 정확히 입력 → 활성.

> ⚠ 로컬 supabase 환경에서는 클릭해도 OK (test user). Cloud 환경에서는 본인 단일 user → 클릭 시 정말 삭제되니 Task 17까지 누르지 말 것.

**Verification:** 모달 동작 + 버튼 활성/비활성 토글.

- [ ] **Step 7: Commit**

```bash
git -C "C:/Dev/Workspace/cadence" add apps/pwa/lib/supabase/admin.ts apps/pwa/app/\(app\)/settings/account-actions.ts apps/pwa/app/\(app\)/settings/account-section.tsx apps/pwa/tests/account-actions.spec.ts
git -C "C:/Dev/Workspace/cadence" commit -m "feat(pwa): 실 계정 삭제 — auth.admin.deleteUser + DELETE confirm + cascade"
```

---

## Task 13: 운영 알림 — routine 실패 → admin Discord [Code]

**Type:** `[Code]` — 작은 wrapper + commit. (Vercel build 실패 알림은 본 task의 "[Manual] sub-step"으로 Vercel Deploy Hook 설정.)

**Files:**
- Create: `apps/routine/src/utils/admin-alert.ts`
- Modify: `apps/routine/src/main.ts` (catch 블록에서 admin-alert 호출)
- Modify: `apps/routine/src/agent.ts` (각 stage의 throw 직전 admin-alert — 옵션, 본 plan에선 main.ts catch 1곳만 필수)
- Modify: `apps/routine/README.md` (env + 알림 1문단)

> **결정:** mcp-discord의 `sendAdminAlertTool`은 이미 webhook POST를 구현. 단, MCP 서버 부팅을 거쳐 호출하면 routine 실행 중 무거우므로, **runtime용 thin wrapper**(같은 webhook을 직접 fetch)를 routine 내 utils로 둔다. mcp-discord 구현과 형식이 동일하면 파급 X. 환경변수 `DISCORD_ADMIN_WEBHOOK_URL`은 Plan 02부터 이미 `.env`에 있음.

- [ ] **Step 1: Write `apps/routine/src/utils/admin-alert.ts`**

```typescript
import { logger } from "./logger.ts";

export type Severity = "info" | "warn" | "error";

interface AlertInput {
  severity: Severity;
  message: string;
  context?: Record<string, unknown>;
}

/**
 * DISCORD_ADMIN_WEBHOOK_URL로 알림 1건 POST. 실패 시 throw 안 함 — log만 (알림 자체가
 * 실패해 routine 흐름이 깨지면 안 되므로 best-effort).
 */
export async function sendAdminAlert(input: AlertInput): Promise<void> {
  const url = process.env.DISCORD_ADMIN_WEBHOOK_URL;
  if (!url) {
    logger.warn({ stage: "admin-alert", reason: "DISCORD_ADMIN_WEBHOOK_URL missing" });
    return;
  }
  const ctxStr = input.context
    ? `\n\`\`\`json\n${JSON.stringify(input.context, null, 2)}\n\`\`\``
    : "";
  const content = `[${input.severity.toUpperCase()}] cadence-routine: ${input.message}${ctxStr}`.slice(0, 1990);
  try {
    const res = await fetch(url, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ content }),
    });
    if (!res.ok) {
      logger.warn({ stage: "admin-alert", status: res.status });
    }
  } catch (err) {
    logger.warn({ stage: "admin-alert", err: (err as Error).message });
  }
}
```

- [ ] **Step 2: Modify `apps/routine/src/main.ts`** — fatal catch에서 admin-alert 호출

```typescript
// 기존:
main().catch((err) => {
  console.error("[routine] fatal:", err);
  process.exit(1);
});

// 다음으로 교체:
import { sendAdminAlert } from "./utils/admin-alert.ts";

main().catch(async (err) => {
  // eslint-disable-next-line no-console
  console.error("[routine] fatal:", err);
  await sendAdminAlert({
    severity: "error",
    message: (err as Error).message ?? "unknown fatal",
    context: {
      stack: ((err as Error).stack ?? "").slice(0, 1500),
      user_id: process.env.COACH_USER_ID,
      ts: new Date().toISOString(),
    },
  });
  process.exit(1);
});
```

- [ ] **Step 3: agent.ts stage warn 알림 (옵션, 본 plan에서는 skip)**

agent.ts 각 stage(get_active_sprint / get_today_backbone / signals / generate / publish)의 try/catch에서 catch 시 warn 알림을 추가하고 throw할 수 있다. 구현 시 호출 빈도/에러 노이즈 고려해 retry exhausted 시점에 1회만. **본 plan에서는 main.ts catch 1곳만 필수**, agent.ts 변경은 미루고 D+7 사용자 회고에서 필요하면 추가.

**Verification:** 본 step은 산출 X (의식적 skip).

- [ ] **Step 4: Vercel Deploy Hook → Discord 알림 [Manual sub-step]**

Vercel Project → Settings → "Integrations" 또는 Settings → Git → "Deploy Hooks" 섹션 확인:

옵션 A — Vercel + Discord 직접 연동:
1. Vercel "Integrations" 마켓플레이스 → "Discord" 검색 → 무료 integration 설치 → 알림할 Discord 채널 선택 → 이벤트(`Deployment Failed`, `Deployment Error`) 체크. 설치 시 Discord OAuth 거쳐 자동 webhook 생성.

옵션 B — 자체 webhook (간단):
1. Vercel Settings → Notifications (또는 Project → Settings → Notifications) → "Configure Slack/Discord webhook URL" → 위 admin webhook URL 붙여넣기 → Deployment Failure만 체크.

옵션 C — Vercel 무료 plan에 위 옵션이 없으면 GitHub Actions 워크플로 (`.github/workflows/notify-deploy-fail.yml`)에서 Vercel Deployment Status webhook을 받아 admin webhook으로 forward. **본 plan에서는 옵션 A를 1차 시도 → 안 되면 옵션 B → C 순서. C는 Plan 07 후보로 미루기 가능.**

**Verification:** 일부러 빌드 실패 commit (예: `apps/pwa/app/page.tsx` 임포트 1개 깨뜨림) push → Vercel Deployments에 "Failed" 표시 + Discord admin 채널에 알림 1건. 검증 후 즉시 revert.

> 본 sub-step은 commit 없음 — Vercel 대시보드 셋업.

- [ ] **Step 5: routine README 갱신**

`apps/routine/README.md`에 다음 1문단 추가 (Cloud 환경 swap + 알림 안내):

```markdown
## 운영 환경

- 환경변수: root `.env` (production = Cloud Supabase). 테스트는 `.env.test` (로컬 Supabase).
- 실패 시 `DISCORD_ADMIN_WEBHOOK_URL`로 1건 알림 (`[ERROR] cadence-routine: …`).
- PC가 7am KST에 켜져 있어야 카드 생성. PC 꺼짐 시 그날 누락 — 1인 dogfooding 한도 내 수용.
```

- [ ] **Step 6: routine 빌드 + lint**

```bash
pnpm --filter @cadence/routine typecheck     # 0 error
pnpm --filter @cadence/routine lint          # 0 error
pnpm --filter @cadence/routine test --run    # 기존 케이스 PASS
```

**Verification:** 0 error. 알림 wrapper에 직접적 vitest는 생략 (mock fetch만 추가하면 1 case 가능 — 시간 절약을 위해 skip).

- [ ] **Step 7: 알림 Live test**

```bash
# .env에 일부러 잘못된 SUPABASE_URL을 설정하거나 SUPABASE_SERVICE_ROLE_KEY를 비워 1회 routine 실행
# 또는 routine 코드에 임시 throw new Error("test alert") 삽입 후 1회 실행 → 즉시 revert
pnpm --filter @cadence/routine start
```

Discord admin 채널에 `[ERROR] cadence-routine: …` 메시지 도착 확인.

**Verification:** 채널에 메시지 1건. context.stack에 trace 일부 표시.

- [ ] **Step 8: Commit**

```bash
git -C "C:/Dev/Workspace/cadence" add apps/routine/src/utils/admin-alert.ts apps/routine/src/main.ts apps/routine/README.md
git -C "C:/Dev/Workspace/cadence" commit -m "feat(routine): fatal 시 admin Discord 알림 — 운영 가시성"
```

---

## Task 14: Lighthouse PWA audit ≥ 90 [Manual]

**Type:** `[Manual]` — 검증 결과 캡처 후 docs commit (Task 15에 흡수).

> Plan 05 task 23 마지막 step에서 로컬 PWA 점수는 측정. Plan 06은 Cloud 배포 후 production URL에서 재측정 — HTTPS / SW / manifest 모두 만족.

- [ ] **Step 1: Chrome DevTools Lighthouse 1회 실행**

PC Chrome → 시크릿 창에서 `<VERCEL_DEPLOY_URL>` 접속 → 로그인 (또는 미로그인 /login 상태로) → DevTools (F12) → Lighthouse 탭:

| 옵션 | 값 |
|---|---|
| Mode | Navigation |
| Device | Mobile |
| Categories | Progressive Web App, Performance, Accessibility, Best Practices |

"Analyze page load" 클릭 → 30초~1분 대기.

**Verification:** Lighthouse 결과:
- PWA: ≥ 90 (목표). Installable / PWA Optimized 둘 다 ✅
- Performance: ≥ 80 (참고)
- Accessibility: ≥ 90 (참고)
- Best Practices: ≥ 90 (참고)

- [ ] **Step 2: PWA 카테고리 세부 항목**

| 체크 | 통과 조건 |
|---|---|
| Installable / Manifest | `name` `short_name` `icons` `start_url` `display: standalone` 모두 OK |
| PWA Optimized — `<meta name="viewport">` | apps/pwa/app/layout.tsx에 있음 (Plan 05) |
| PWA Optimized — themed splash | `theme_color` `background_color` 일치 (Plan 05) |
| Service worker registers | `/sw.js` activated |
| HTTPS | Vercel 자동 |
| icons 192 + 512 | `public/icons/` (Plan 05) |
| start_url responds with 200 | `/` 또는 `/login` |

**Verification:** 위 7항목 모두 ✅.

- [ ] **Step 3: 점수 캡처 (스크린샷 또는 점수 기록)**

스크린샷 파일을 `docs/operations/lighthouse-2026-04-29.png`로 저장 (선택). 또는 README/CHANGELOG에 점수 기록.

**Verification:** 본 plan에서는 점수 ≥ 90이면 OK. 미달 시 원인 파악 후 수정 (대부분 manifest 메타 누락).

- [ ] **Step 4: 수치 미달 시 디버그 흐름**

| Issue | 진단 | 수정 |
|---|---|---|
| `start_url` 404 | manifest의 start_url 경로 오타 | `/` 또는 `/login`로 통일 |
| icons 누락 | `public/icons/` 미배포 | Vercel 빌드 로그에서 정적 자산 포함 확인 |
| SW 미등록 | next.config.mjs `withSerwist` 누락 | Plan 05 task 17 재확인 |
| viewport 없음 | layout.tsx 메타 누락 | Plan 05 task 16 재확인 |

본 plan은 코드 변경 없는 검증 task — 미달 시 Plan 05로 회귀 fix → 재 deploy.

---

## Task 15: 계정 삭제 운영 검증 (실 데이터 1회) [Manual]

**Type:** `[Manual]` — 본인이 직접 cloud 계정을 삭제 → 재가입까지 1회 라운드. Discord 보안 검증.

> Task 12에서 코드는 다 짜였음. 본 task는 **그 코드를 cloud 환경에서 한 번 정말 실행**해 cascade가 6 테이블 모두 정리하는지 확인. 단, 본인 단일 user이므로 직후 재가입 + Onboarding 다시 거쳐야 함. 시간 ~5분.

- [ ] **Step 1: 사전 export**

`<VERCEL_DEPLOY_URL>/settings` → "데이터 내보내기 (JSON)" → 파일 저장 → 6 테이블 row 수 메모.

**Verification:** JSON 파일에 본인 데이터 보존.

- [ ] **Step 2: 계정 삭제 실행**

같은 페이지 → "계정 삭제" → 모달 → "DELETE" 입력 → "영구 삭제" → 처리 → /login으로 redirect (`?deleted=1`).

**Verification:**
- /login으로 redirect됐다.
- Cloud Studio → Authentication → Users → 본인 row 사라짐.
- Cloud Studio → Table Editor → 모든 6 테이블 → 본인 user_id row 0개 (cascade 통과).

- [ ] **Step 3: 재가입**

같은 이메일로 매직링크 → /onboarding → 4-step 다시 → /today.

**Verification:** Onboarding 다시 시작 (이전 데이터 흔적 없음). /today 빈 상태.

- [ ] **Step 4: routine 재실행 (Task 5처럼)**

새 user_id를 `.env` `COACH_USER_ID`에 채움 → routine 1회.

**Verification:** Cloud daily_cards 1개 row.

> 이 시점에서 Plan 06 코드 동작 100% 검증 완료. 데이터는 빈 상태부터 다시 쌓임.

---

## Task 16: cloud-runbook.md 작성 + 사용자 영구 보관 [Manual + Code mix]

**Type:** `[Manual]` planning + `[Code]` (md 파일 작성 + commit).

**Files:**
- Create: `docs/operations/cloud-runbook.md`

> Plan 06의 Manual task들을 운영 중 다시 보고 싶을 때를 위한 runbook 1개 파일. 간결 (3~5분 readout).

- [ ] **Step 1: Write `docs/operations/cloud-runbook.md`**

```markdown
# Cadence Cloud 운영 Runbook (Plan 06 산출)

## 1. 배포 트리거
- main 브랜치 push → Vercel 자동 deploy.
- Production URL: `https://cadence-bpx27.vercel.app` (실 URL은 본인 Vercel 대시보드 참조).

## 2. 환경 매트릭스
| 컴포넌트 | URL | 자격 |
|---|---|---|
| PWA (production) | Vercel | `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (Vercel env) + `SUPABASE_SERVICE_ROLE_KEY`(Production만) |
| Routine (PC 24/7) | localhost cron | root `.env` (Cloud 자격) |
| Tests | local Supabase | 각 패키지 `.env.test` |

## 3. 자주 하는 ops
### 매직링크 안 옴
1. Supabase Cloud Studio → Authentication → Logs → 메일 발송 row 확인.
2. URL Configuration의 Site URL이 Vercel URL인지 확인 (Plan 06 Task 9).
3. Spam 폴더.

### Vercel 빌드 실패
- Deployments → 해당 deployment → Build Logs → 첫 error 라인 검색.
- 가장 흔한 원인: pnpm-lock.yaml과 코드 불일치 → `pnpm install`로 lockfile 갱신 → 재 push.

### Routine이 어제 카드 미INSERT
- PC 켜졌는지 + cron 정상인지 확인.
- `apps/routine/logs/`의 최근 로그.
- Discord admin 채널에 `[ERROR] cadence-routine: …` 메시지 있는지.

### 본인 데이터 백업
- /settings → "데이터 내보내기 (JSON)" → 안전한 곳에 저장.
- Supabase 자체 자동 백업: Free tier 7일 보관 (Cloud Studio → Database → Backups).

## 4. 비용 모니터링
| 항목 | 한도 (Free) | 1인 사용량 추정 | 점검 URL |
|---|---|---|---|
| Supabase DB | 500 MB | < 1 MB | Cloud Studio → Settings → Usage |
| Supabase Auth MAU | 50,000 | 1 | 동일 |
| Supabase Egress | 5 GB/월 | < 50 MB | 동일 |
| Vercel Bandwidth | 100 GB/월 | < 100 MB | Vercel Settings → Usage |
| Vercel Build | 6,000분/월 | < 60분 | 동일 |
| Anthropic API | (사용량 별도) | 매일 1회 routine + cache | Anthropic Console |

월 1회 위 6개 점검. 50% 도달 시 spec § 11 D+30 트리거 검토.

## 5. 도메인 이전 (선택, D+30)
현재는 vercel.app 서브도메인. 자체 도메인 시:
1. Vercel → Domains → Add → 본인 도메인 입력 → DNS 안내대로 A/CNAME 설정.
2. Cloud Auth → URL Configuration → Site URL을 새 도메인으로 갱신.
3. Redirect URLs에 새 도메인의 `/auth/callback` 추가.
4. 기존 vercel.app은 유지 (auto redirect 또는 fallback).

## 6. 비상 복구 (DB 손상)
Free tier 자동 백업(7일):
1. Cloud Studio → Database → Backups → 시점 선택 → "Restore".
2. 복구 후 routine + PWA 동작 1회 점검.
3. Restore가 안 되는 경우 (8일 이상 전 데이터): 본인 export JSON을 `cadence init` + 수동 INSERT로 재구성. 자동화 안 됨 — 1인 도구 한도.
```

- [ ] **Step 2: Commit**

```bash
git -C "C:/Dev/Workspace/cadence" add docs/operations/cloud-runbook.md
git -C "C:/Dev/Workspace/cadence" commit -m "docs(ops): cloud-runbook — Plan 06 운영 절차 영구 보관"
```

---

## Task 17: .env.example 갱신 + pwa-deploy.md 보강 [Code]

**Type:** `[Code]` — 작은 doc + commit.

**Files:**
- Modify: `.env.example` (production 값 주석 + .env.test 분리 안내)
- Modify: `docs/operations/pwa-deploy.md` (Plan 06 실 적용 결과 1문단)

- [ ] **Step 1: Modify `.env.example`** — Cloud 안내 commenting

기존 `# Plan 04 — apps/pwa (브라우저 노출, anon 권한 + RLS)` 섹션 위에 다음 추가:

```ini
# ============================================================
# Plan 06 — Cloud 배포 안내
# ============================================================
#
# 이 .env는 *production = Supabase Cloud* 자격을 보관합니다.
# - apps/routine: 이 파일을 dotenv로 로드 → Cloud DB로 카드 INSERT.
# - apps/pwa (Vercel): 별도 Vercel env vars로 같은 Cloud 자격을 등록 (이 파일 미사용).
# - 테스트(vitest): 각 패키지의 .env.test가 로컬 Supabase 자격을 보관 → 격리.
#
# 환경 swap 절차: docs/operations/cloud-runbook.md
```

- [ ] **Step 2: Modify `docs/operations/pwa-deploy.md`** — 실값 보강

§ 7 "Plan 06 (실 배포)에서 추가될 항목" 아래에 다음 1문단 추가:

```markdown

---

### Plan 06 적용 결과 (2026-04-29 기준)

- Production URL: `https://cadence-bpx27.vercel.app` (실 URL은 본인 Vercel 대시보드 참조)
- Supabase Cloud project: Northeast Asia (Seoul)
- 실 적용 항목: 데이터 export · 실 계정 삭제 · routine fatal admin 알림 · Vercel Deploy 알림.
- 운영 절차 + 비용 모니터링: `docs/operations/cloud-runbook.md`
- 도메인은 vercel.app 서브도메인으로 충분 (1인 도구). 자체 도메인은 cloud-runbook.md § 5.
```

- [ ] **Step 3: Commit**

```bash
git -C "C:/Dev/Workspace/cadence" add .env.example docs/operations/pwa-deploy.md
git -C "C:/Dev/Workspace/cadence" commit -m "docs(ops): .env.example + pwa-deploy.md — Plan 06 실 배포 결과 반영"
```

---

## Task 18: README 갱신 + Plan 06 게이트 + tag [Manual + Code]

**Type:** `[Code]` — README 수정 + commit + tag.

**Files:**
- Modify: `README.md` (root) — Plan 06 산출 1문단
- Modify: `apps/pwa/README.md` — production URL 추가 (선택)

- [ ] **Step 1: root `README.md` Plan 06 1문단**

기존 README 어딘가의 "현황" 또는 "Plans" 섹션에:

```markdown
- ✅ Plan 06 — Vercel + Supabase Cloud 배포 + 데이터 export + 계정 삭제 + 운영 알림 (`docs/superpowers/plans/2026-04-29-06-deploy.md`)
  - Production: `https://cadence-bpx27.vercel.app`
  - Runbook: `docs/operations/cloud-runbook.md`
```

- [ ] **Step 2: `apps/pwa/README.md` (선택)**

상단 1줄 "Production: …" 추가.

- [ ] **Step 3: Plan 06 게이트 — 다음 항목 모두 통과**

```bash
pnpm --filter @cadence/pwa typecheck    # 0 error
pnpm --filter @cadence/pwa lint          # 0 error
pnpm --filter @cadence/pwa test          # 29 PASS (Plan 05 25 + Plan 06 4)
pnpm --filter @cadence/pwa build         # success + public/sw.js 생성
pnpm --filter @cadence/routine typecheck # 0 error
pnpm --filter @cadence/routine lint      # 0 error
pnpm --filter @cadence/routine test      # 기존 케이스 PASS
```

수동 검증:
- [ ] Vercel production URL 200 + /login + /today 정상
- [ ] 폰 PWA 설치 + 매직링크 로그인 + Onboarding + Today 동작
- [ ] /settings → 데이터 내보내기 → JSON 다운로드 + 8 키 OK
- [ ] /settings → 계정 삭제 모달 (실제 클릭은 Task 15에서 1회 완료)
- [ ] routine 1회 실행 → Cloud daily_cards INSERT + 에러 시 admin Discord 알림
- [ ] Lighthouse PWA ≥ 90
- [ ] Cloud Studio Backups → 자동 백업 row 1개 이상 (Day 2 이후)

- [ ] **Step 4: Tag**

```bash
git -C "C:/Dev/Workspace/cadence" add README.md apps/pwa/README.md
git -C "C:/Dev/Workspace/cadence" commit -m "docs: README — Plan 06 산출 + production URL"
git -C "C:/Dev/Workspace/cadence" tag plan-06-complete
git -C "C:/Dev/Workspace/cadence" push origin plan-06-complete
```

**Verification:** `git tag --list` 결과에 `plan-06-complete`. GitHub Releases에서도 보임.

---

## Self-Review

- [x] **Spec coverage**:
  - § 8.1 (환경) — Vercel free + Supabase free + routine PC 로컬, 비용 ~$0 모두 충족.
  - § 8.2 (환경 변수) — `NEXT_PUBLIC_*` (PWA) vs `SUPABASE_SERVICE_ROLE_KEY` (routine + admin route) 분리 + Vercel env vars Production-only 격리. spec 표 그대로.
  - § 8.3 (모니터링) — routine 실패 → admin Discord 알림 (Task 13). Routines audit log는 Plan 03 그대로. cloud-runbook.md § 4에 비용 점검 명시.
  - § 4.3 (RLS) — anon 권한으로 export 가능 (auth.uid() 자동). admin client는 deleteUser 단일 라우트에 한정 + server-only 가드.
  - § 7.3 (Frontend 테스트) — vitest 4 case 추가 (export schema 2 + delete confirm 2). e2e는 데이터 손실 위험으로 자동 X — Task 15 수동 1회.

- [x] **Placeholder scan**:
  - 도메인 — vercel.app 서브도메인 채택, 자체 도메인은 cloud-runbook § 5에 D+30 옵션으로 명시.
  - GitHub Actions cron — Plan 07 후보로 명시 분리 (Plan 06 EXCLUDE).
  - agent.ts stage warn 알림 — Task 13 Step 3에서 의식적 skip + D+7 회고 트리거 명시.
  - Vercel Deploy Hook 옵션 C(GitHub Actions forward) — 옵션 A/B 실패 시 fallback. Plan 07 후보 명시.

- [x] **Type consistency**:
  - `getSupabaseAdminClient()` 반환은 `SupabaseClient` (any-DB). RLS 우회 클라이언트라 generic Database 타입 X — 의도적.
  - `UserExport` 인터페이스는 6 테이블 raw row를 `unknown[]`로 보관 — JSON 직렬화만 하면 되므로 타입 안전성보다 단순성 우선. 검증은 schema 테스트로.
  - Server Action `deleteAccount`의 반환 타입 `{ ok: false; … } | never` — redirect()가 throw로 동작해 클라이언트는 reject 분기만 받는 패턴.

- [x] **Dependencies**:
  - **상위 plan**: Plan 04 (PWA Settings/Auth), Plan 05 (manifest/SW + Settings 폼 + 계정 placeholder), Plan 02 (mcp-discord — sendAdminAlertTool 패턴 참조), Plan 03 (routine + admin webhook env), Plan 01 (cascade FK).
  - **하위 plan**: Plan 07 후보 (routine을 GitHub Actions cron 또는 Edge Function으로 이전, 자체 도메인, 멀티유저).
  - **외부 인프라**: Supabase Cloud Free tier · Vercel Hobby · Discord webhook · GitHub OAuth (Vercel).

- [x] **Plan 05 회귀**:
  - SettingsForm의 placeholder section 1개만 제거. 다른 form section(GitHub/Discord/notify/Sprint reset) 변경 X.
  - manifest/SW/icons 변경 X — Plan 05 산출 그대로 Vercel에 배포.
  - 25 vitest + 2 Playwright PASS 유지 (Plan 06에서 4 vitest 추가 → 누계 29).

- [x] **EXCLUDED 항목 검증**:
  - 웹푸시 → 없음. D+30.
  - 멀티유저/팀 → 없음. 1인 도구.
  - LLM 회귀 (Promptfoo) → 없음. D+30.
  - 자체 도메인 → cloud-runbook § 5에 옵션만 명시.
  - GitHub Actions cron / Edge Function 이전 → Plan 07 후보 명시.
  - 자체 백업/복구 자동화 → Supabase Free 자동 백업 7일에 위임 (cloud-runbook § 6).

---

## Plan 06 산출물 요약

- **Cloud 인프라** — Supabase Cloud project (Seoul) + 6 테이블 + RLS · Vercel project + production URL · Auth Site URL 갱신 · Vercel env vars 3개 (anon + url + service_role)
- **데이터 export** — `lib/queries/user-export.ts` + `app/api/account/export/route.ts` + Server Action `previewExport` + AccountSection 버튼 · 8 키 JSON · RLS 통과 anon 권한
- **계정 삭제** — `lib/supabase/admin.ts` (server-only) + Server Action `deleteAccount` + AccountSection Confirm modal ("DELETE" 입력) + signOut + /login redirect · cascade로 6 테이블 정리
- **운영 알림** — `apps/routine/src/utils/admin-alert.ts` (DISCORD_ADMIN_WEBHOOK_URL 직접 fetch) + main.ts catch에서 호출 · Vercel Deploy Hook → Discord (Task 13 Step 4)
- **환경 swap** — `.env`(Cloud production) ↔ `.env.test`(local 테스트) 분리 명시 + `.env.example` 주석 갱신
- **runbook** — `docs/operations/cloud-runbook.md` (배포 트리거 / 환경 매트릭스 / 자주 하는 ops / 비용 모니터링 / 도메인 이전 / 비상 복구)
- **검증** — Lighthouse PWA ≥ 90 production URL · 폰 매직링크 + PWA 설치 + Onboarding 풀 사이클 · 계정 삭제 1회 라운드(export → delete → 재가입)
- **Vitest 추가** — account-actions 4 case (export schema 2 + delete confirm 2). 누계 29 (Plan 04: 11 / Plan 05: 14 / Plan 06: 4)
- **Plan 06 코드 task 3개** (11/12/13) + **수동 ops task 14개** (1~10, 14, 15, 16, 17, 18) — 총 18 tasks
- 예상 작업 시간: **6~10h** (manual ops 4~6h + code 2~4h)
